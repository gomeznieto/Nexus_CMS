@model IEnumerable<ListPostViewModel>
@{
	ViewData["Title"] = "Entradas";
}

<h1>@ViewBag.Format</h1>
<a asp-action="Crear" asp-route-format="@ViewBag.Format" class="btn btn-primary">Crear nuevo @ViewBag.Format </a>
	@foreach (var format in Model)
	{
		<table class="table">

			<thead>
				<tr>
					<th>Titulo</th>
					<th>Categoria</th>
					<th>Formato</th>
					<th>Creación</th>
					<th>Acciones</th>
				</tr>
			</thead>

			<tbody>
				@foreach (var post in format.posts)
				{
					if(format.posts.Count() != 0)
					{
						<tr>
							<td>@post.title</td>
							<td>@post.category</td>
							<td>@post.format</td>
							<td>@post.created_at.ToShortDateString()</td>
							<td>
							<a class="btn btn-primary" asp-action="Editar" asp-route-id="@post.id" asp-route-format="@ViewBag.Format">Editar</a>
								<a class="btn btn-danger" onclick="openModal({id: '@post.id', name: '@post.title'})" )>Borrar</a>
							</td>
						</tr>
					}
				}
			</tbody>

		</table>
	}

<!-- MODAL -->
<partial name="_Modal" model='new { url = "/Posts" }' />
